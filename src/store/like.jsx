import { atomFamily } from "recoil";

export const likeCountFamily = atomFamily({
    key: "likeCountFamily",
    default: 0
});

// export   const likeNameFamily = atomFamily({
//     key: "likeNameFamilyike",
//     default: 0
// })